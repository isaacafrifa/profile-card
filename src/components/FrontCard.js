import ProfileImage from "../assets/images/avatar.jpg";

const FrontCard = (props) => {
  return (
    <div className="" onMouseEnter={props.handleEvent} onMouseLeave={props.handleEvent} onClick={props.handleEvent}>


    </div>
  );
}

export default FrontCard;